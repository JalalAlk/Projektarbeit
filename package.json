{
  "name": "c-users-alkhatba-onedrive---htwsaar-de-desktop-proejkt-1",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "clean": "rimraf ./dist && rimraf ./playwright-report",
    "lint": "eslint . && stylelint 'src/**/*.{astro,css}'",
    "lint:fix": "eslint . --fix && stylelint 'src/**/*.{astro,css}' --fix",
    "prebuild": "npm run clean && npm run lint",
    "build": "astro build",
    "predev": "npm run clean && npm run lint",
    "dev": "astro dev",
    "preview": "astro preview",
    "start": "astro dev",
    "serve": "astro preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "format": "prettier --write ."
  },
  "dependencies": {
    "open": "^10.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.14.2",
    "astro": "^4.11.0",
    "astro-eslint-parser": "^1.0.2",
    "concurrently": "^8.0.1",
    "eslint": "^9.4.0",
    "eslint-plugin-astro": "^1.2.2",
    "globals": "^15.4.0",
    "postcss-html": "^1.7.0",
    "prettier": "^2.8.4",
    "rimraf": "^5.0.1",
    "stylelint": "^16.6.1",
    "stylelint-config-html": "^1.1.0",
    "stylelint-config-standard": "^36.0.1"
  }
}